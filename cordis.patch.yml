# ohdsh-api-facade bundle layer.
#
# `dsh plugin add ohdsh-api-facade` applies this patch to the composed config.
# Every option has a schema default — override a row key in your own profile
# patch layer instead of editing this file.
- insert:
    - id: ohdsh-api-facade
      name: ohdsh-api-facade
